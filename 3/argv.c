#include <stdio.h>
int main(int argc, char** argv) {

//agrc->argument count
//argv->argument vector

    printf("参数的个数是%d\n", argc);
    printf("第一个参数是： %s\n", argv[0]);
    printf("第二个参数是： %s\n", argv[1]);
    printf("第三个参数是： %s\n", argv[2]);
    printf("第四个参数是： %s\n", argv[3]);

    return 0;
    





}